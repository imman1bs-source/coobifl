<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <%- include('../partials/header') %>

    <main>
        <!-- Hero Section with Google-style Search -->
        <section class="hero-section py-5">
            <div class="container text-center">
                <h1 class="display-4 mb-4">Coobifl</h1>
                <p class="lead mb-5">Discover where products are made and what they're made of</p>

                <!-- Google-style Centered Search Bar -->
                <div class="search-container mx-auto" style="max-width: 600px;">
                    <form action="/search" method="GET" class="search-form">
                        <div class="input-group input-group-lg">
                            <span class="input-group-text bg-white">
                                <i class="fas fa-search text-muted"></i>
                            </span>
                            <input
                                type="text"
                                class="form-control border-start-0"
                                name="q"
                                placeholder="Search for products..."
                                autocomplete="off"
                            >
                            <button class="btn btn-primary px-4" type="submit">
                                Search
                            </button>
                        </div>
                    </form>
                </div>

                <!-- Add Product Button -->
                <div class="mt-4">
                    <a href="/add-product" class="btn btn-success btn-lg">
                        <i class="fas fa-plus-circle"></i> Add Product
                    </a>
                </div>
            </div>
        </section>

        <!-- Featured Products Section (placeholder for Phase 5) -->
        <section class="py-5 bg-light">
            <div class="container">
                <h2 class="text-center mb-4">Featured Products</h2>
                <p class="text-center text-muted">Coming soon - Featured products will appear here</p>
            </div>
        </section>
    </main>

    <%- include('../partials/footer') %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/main.js"></script>
</body>
</html>
